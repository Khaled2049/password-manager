import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.khaled2049.password',
  appName: 'password-manager',
  webDir: 'www'
};

export default config;
